import React from "react";

const Loader = () => {
  return (
    <div style={{ color: "#3498db", textAlign: "center", margin: "auto" }}>
      Loading...
      <div style={{ textAlign: "center", margin: "auto" }} class="loader"></div>
    </div>
  );
};

export default Loader;
